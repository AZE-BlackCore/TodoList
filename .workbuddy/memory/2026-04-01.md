# 2026-04-01 工作日志

## v1.0.0 发布（00:30）

### 操作记录
- 确认两端（GitHub/GitCode）main 分支均为最新
- 打 `v1.0.0` tag
- 推送 tag 到 GitHub → 触发 `release.yml` 工作流，自动构建 Windows 便携版发布到 GitHub Releases
- 推送 tag 到 GitCode（GitCode hooks 检查通过）

## ESLint 全量修复（00:42）

### 修复范围：83 个问题 → 0 问题（77 errors + 6 warnings → 全清）

**修复类型一：`no-explicit-any` (any 类型)**
- `vite-env.d.ts`：ElectronAPI 所有方法 any → `Record<string, unknown>`，新增 `sendNotification?` 声明
- `taskStore.ts` / `projectStore.ts`：6+4 处 `catch(error: any)` → `catch(error: unknown) + instanceof Error`
- `viewConfigStore.ts`：移除未用的 `get` 参数
- `Layout.tsx`：`ICON_MAP: Record<string, any>` → `Record<string, React.ComponentType<{className?: string}>>`
- `TaskReminder.tsx`：`(window as any).electronAPI` → `window.electronAPI`
- `ProjectEditDialog.tsx`：`value: any` → `Project[keyof Project]`
- `SettingsPanel.tsx`：2处 catch any → unknown
- `DashboardView.tsx`：StatCard props any → 明确接口 `StatCardProps`
- `GanttView.tsx`：`startOf(...as any)` 和 `getTaskPosition(task: any)` → 类型安全
- `ProjectView.tsx`：`as any` → 具体类型断言
- `cache.ts`：`Map<string, CacheEntry<any>>` → `<unknown>`
- `export.ts`：3处 catch any → unknown
- `main.tsx`：`(window as any).electronAPI` → `window.electronAPI`

**修复类型二：`no-unused-vars` (未使用变量)**
- `Layout.tsx`：移除 Menu、ViewItem、Project 导入
- `ProjectCard.tsx`：移除 MoreVertical 导入
- `TaskDetailPanel.tsx`：移除 useState、useEffect 导入
- `TaskEditDialog.tsx`：移除 register 解构
- `ViewOrderSettings.tsx`：移除空接口+空解构，改为无参数签名
- `viewConfigStore.ts`：移除未用的 `get`
- `ListView.tsx`：移除 Filter/MoreVertical/TableSkeleton，移除 getStatusLabel 函数
- `KanbanView.tsx`：移除 useState/editingTask/showNewTaskDialog/handleDrop/updateTaskStatus
- `SubtaskList/TagManager/TimeTracker`：`taskId` → `_taskId` + eslint-disable
- `VirtualList.tsx`：`getStatusLabel` → `_getStatusLabel` + eslint-disable；移除循环变量 i

**修复类型三：`react-hooks/exhaustive-deps` (missing deps)**
- Layout/CalendarView/DashboardView/KanbanView/ListView/ProjectView：补全 fetchTasks/fetchProjects/loadViews 到 deps 数组

### 提交：703644f — fix: resolve all ESLint errors and warnings (83 issues fixed)
### 推送：GitCode ✅，GitHub ❌（网络连接超时，代码无误，等网络恢复再推）

## 推送修复提交（00:40）
- 提交：`fix: fix Recharts Tooltip formatter type error in DashboardView`（d809c7c）
- 推送到 GitHub 和 GitCode main 分支均成功

## TypeScript 类型修复（00:40）

### 修复的文件
`renderer/src/views/DashboardView.tsx`

### 问题 1：Recharts Tooltip formatter 类型错误
- 原因：`formatter={(value: number) => ...}` 中 `value` 实际类型是 `ValueType | undefined`
- 修复：去掉手动类型标注，改为 `formatter={(value) => [`${value ?? 0} 个任务`, '数量']}`

### 问题 2：Tooltip 上使用了不存在的 nameKey 属性
- 原因：`nameKey` 是 `<Pie>` 的属性，不是 `<Tooltip>` 的属性
- 修复：直接删除 `Tooltip` 上的 `nameKey="name"`（`<Pie>` 默认 nameKey 就是 `name`）

### 关键信息
- GitHub Releases 页面：https://github.com/AZE-BlackCore/TodoList/releases
- Actions 页面（查看构建进度）：https://github.com/AZE-BlackCore/TodoList/actions
- 两个远端：`github` = GitHub，`origin` = GitCode
